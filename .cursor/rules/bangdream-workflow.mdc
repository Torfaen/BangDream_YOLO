---
description: BangDream YOLO 项目流程、需求文档门禁与代码风格（不过度设计）
alwaysApply: true
---

# BangDream_yolo 工作方式

## 开发计划

- **开发计划**：以 Cursor Plan **bangdream-yolo-autoplay**（里程碑 m0–m8）为权威路线图；本地副本一般在 Cursor 的 `.cursor/plans/` 下。开工前指明当前里程碑 ID，并与 Plan 内 todo 对齐。
- 可选：在仓库 `docs/development-plan.md` 放与上述 Plan 同步的摘要，便于无 Cursor 或协作时查阅。

## 每次更新：先文档、后代码

- 在 `docs/requirements/` 新增 **`REQ-YYYYMMDD-主题.md`**（一天多份可加后缀 `-a`、`-b`）。
- 文档至少包含：**背景/目标**、**范围（明确不做的事）**、**技术细节**（接口、配置项、依赖版本、与 nemu_ipc / minitouch / YOLO 的边界）、**验收标准**、**风险与回滚**。
- **在用户确认该 REQ 的技术细节之后**，再改代码；未确认的用 `TODO` 标在 REQ 里，不猜实现。

## 代码与注释

- **要注释**：文件/模块顶部一句话职责；非一眼能懂的公式、坐标变换、时序与状态机分支；对外可调用函数的参数与返回值（简短即可）。
- **不要**：为「通用框架」拆很多层、空接口、过度配置化；能用函数 + 数据结构解决的，不要上复杂模式。
- **偏好**：短函数、直白控制流、少魔法常量（必要的集中放在模块常量或配置）。

## 语言

- 与用户沟通、REQ 文档正文：**简体中文**。
