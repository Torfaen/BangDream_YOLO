[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bangdream-yolo"
version = "0.1.0"
description = "Computer vision helper for BanG Dream on MuMu Player 12."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
